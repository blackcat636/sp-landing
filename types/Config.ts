export default interface Config {
    ripple: boolean,
    darkTheme: boolean,
    inputStyle: string,
    menuMode: string,
    theme: string,
    scale: number,
    activeMenuItem: boolean
}
export default interface layoutState {
    ripple: boolean,
    darkTheme: boolean,
    inputStyle: string,
    menuMode: string,
    theme: string,
    scale: number,
    activeMenuItem: boolean
}
